#!/usr/bin/env python3

airports = [('O’Hare', 'ORD'), ('Los Angeles', 'LAX'), ('Dallas/Fort Worth', 'DFW'), ('Denver', 'DEN')]

for (airport, code) in airports:
    print("The code for {} International Airport is {}.".format(airport, code))
    
